# 数据结构学习笔记
##　gitlet:
- .gitlet(folder)
  - objects(folder)
    - commmit(file)
    - blob(file)
  - refs(folder)
    - heads(folder)
      - master
      - other
  - HEAD(记录当前commit的id)
  - stage(folder)
    - addstage
    - rmemovestage